/**
 * @format
 */

import {AppRegistry} from 'react-native';
import SafetyNoticeApp from './SafetyNoticeApp';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => SafetyNoticeApp);
